# Spotify Developer Dashboard -> your app's Client ID
VITE_SPOTIFY_CLIENT_ID=

# Must exactly match a Redirect URI registered in Spotify Developer Dashboard.
# For a deployed web app, use e.g. https://your-domain.example/callback
# Leave empty to use `${window.location.origin}/callback` during local development.
VITE_SPOTIFY_REDIRECT_URI=
